import React, { PropTypes } from 'react'

const App = React.createClass({
  render () {
    return (
      <diva className='twst'>
        {this.props.a}
      </diva>
    )
  }
})

export default App
